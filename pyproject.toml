[project]
name = "pyre"
version = "1.0.0.dev5"
requires-python = ">=3.13"
dependencies = [
    "gnureadline>=8.3.3",
]

[project.scripts]
pyre = "pyre.main:main"

[tool.uv]
package = true

[tool.setuptools.package-data]
"pyre.assets" = ["*.template"]
